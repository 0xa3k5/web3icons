import * as path from "path";

export const SVG_SOURCE_DIR = path.join(__dirname, "./svgs");
export const SVG_OUTPUT_DIR = path.join(
  __dirname,
  "../../react/icons"
);
export const JSX_OUTPUT_DIR = path.join(
  __dirname,
  "../../react/src"
);
