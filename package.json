{
  "name": "web3icons",
  "description": "monorepo for web3 icons",
  "private": false,
  "author": "0xa3k5",
  "scripts": {
    "build": "turbo build && bun run post-build",
    "build:packages": "bun run build:common && bun run build:core && bun run build:react",
    "build:apps": "bun run build:website && bun run build:figma",
    "build:common": "bun run build --filter @web3icons/common",
    "build:core": "bun run build --filter @web3icons/core",
    "build:react": "bun run build --filter @web3icons/react",
    "build:website": "bun run build --filter @web3icons/website",
    "build:figma": "bun run build --filter @web3icons/figma-plugin",
    "build:api": "bun run build --filter @web3icons/api",
    "post-build": "bun run generate-icons-md && bun run lint && bun run format",
    "dev": "turbo dev",
    "dev:api": "bun run dev --filter @web3icons/api",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,md,mdx,json}\" --log-level error",
    "add-icons": "bun run ./scripts/cli/add-icons.ts",
    "delete-icons": "bun run ./scripts/cli/delete-icons.ts",
    "check-missing": "bun run ./scripts/cli/check-missing-metadata.ts",
    "generate-icons-md": "bun run ./scripts/maintenance/generate-icons-md.ts",
    "update-gecko-networks": "bun run ./scripts/gecko/get-networks.ts",
    "update-gecko-coins": "bun run ./scripts/gecko/get-coins.ts",
    "cdn:sync": "bun run ./scripts/sync-to-r2.ts"
  },
  "devDependencies": {
    "@web3icons/eslint-config": "*",
    "prettier": "^3.3.3",
    "turbo": "v2.0.11",
    "@changesets/cli": "^2.27.7",
    "@aws-sdk/client-s3": "^3.478.0",
    "dotenv": "^17.2.1",
    "@clack/prompts": "^0.9.1",
    "chalk": "^5.4.1",
    "cheerio": "^1.0.0-rc.12",
    "svgo": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.0.33",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "resolutions": {
    "typescript": "5.3.3",
    "esbuild": "^0.19.12"
  },
  "dependencies": {
    "changesets": "^1.0.2"
  }
}
